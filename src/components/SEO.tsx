import { Helmet } from "react-helmet";

const SEO = () => {
  return (
    <Helmet>
      <title>wntn music</title>
      <meta
        name="description"
        content="exclusives neverlane's tracks only in wntn music"
      />
    </Helmet>
  );
};

export default SEO;
